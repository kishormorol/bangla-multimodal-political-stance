"""Bangla multimodal political stance detection.

The package name `bmpb` predates the rename and is kept: it is the installed
distribution and the import path in every config, notebook and prediction file.
The label mapping lives in `bmpb.data.ingest.LABEL_NAMES` and is frozen.
"""

__version__ = "0.1.0"
